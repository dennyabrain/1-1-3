function initCanvas(){
	var t=new thing();
	t.init();
	t.draw();
}

function drawStuff(x,y){
	/*context.beginPath();
	context.moveTo(x, y);
	context.lineTo(450, 50);
	context.stroke();*/
}

function temp(arg){
	/*console.log("in draw.js, function temp " + arg);
	drawStuff(arg.analogValue*100,300);*/
}